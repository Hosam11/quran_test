name: quran_test
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_localizations:
    sdk: flutter

  cupertino_icons: ^1.0.2
  dio: ^4.0.0
  equatable: ^2.0.2
  fimber: ^0.6.1
  json_serializable: ^6.1.5
  dropdown_search: ^2.0.1
  provider: ^6.0.2
  google_fonts: ^2.1.0
  in_app_update: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.0.4
  pedantic: ^1.11.0

flutter:


  uses-material-design: true

  assets:
    - assets/files/
    - assets/images/

  fonts:
    - family: Me Quran
      fonts:
        - asset: fonts/me_quran.ttf

